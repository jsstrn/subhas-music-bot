| #[i Album:] #{title}
| #[i Artist:] #{name}
|
| #[i Description:] #{description} 
|
| ➡️ #[a(href='example.com') Lyrics]
|
| 🎧 #[i Tracks]
|
each track, index in tracks 
  |   #{index + 1}. #{track.title}
  |
|
| 🏆 #[i Credits]
|
each person in credits
  |   · #{person.role} | #{person.name}
  |
|
